const initialState = {
    products: [
    
    ],
};
  
const testimonials_objects = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default testimonials_objects;